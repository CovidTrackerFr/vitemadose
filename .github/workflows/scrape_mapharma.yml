---
name: Scrap Mapharma

on:
## Disabled. Enable if emergency
 #  push:
 #    branches:
 #      - data-auto
 #  schedule:
 #    - cron: "*/5 * * * *"
  push:
    branches:
      - ft/scraperpool # while working on this
  workflow_dispatch:

jobs:

  scrape_keldoc:

    name: "Scrape Mapharma"
    runs-on: "ubuntu-20.04"

    steps:
      - uses: "actions/checkout@v2"
        with:
            token: ${{ secrets.PAT_GRZ }}
      - uses: "actions/setup-python@v2"
        with:
          python-version: "3.8"
      - name: "Install"
        run: scripts/install
      - name: "Scraping Mapharma..."
        run: scripts/scrape -p mapharma
      - name: Switch to data-auto
        run: git switch data-auto
      - uses: stefanzweifel/git-auto-commit-action@v4
        with:
            commit_message: Automatic Update
            file_pattern: data/output/pool/mapharma.json
            push_options: '--force HEAD:data-auto'
